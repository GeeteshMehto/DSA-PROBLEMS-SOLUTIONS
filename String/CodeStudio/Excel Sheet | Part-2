/*
Question Link :- https://www.naukri.com/code360/problems/excel-sheet-part-2_981257

#include <bits/stdc++.h> 
long long titleToNumber(string str) {
    long long ans = 0;
    for (int i = 0; i < str.size(); i++) {
        ans = ans * 26 + (str[i] - 'A' + 1);
    }
    return ans;   
}
